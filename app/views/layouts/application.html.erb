<!DOCTYPE html>
<html>
<head>
  <title>BlogApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <ul>
    <% if logged_in? %>
      <li><%= link_to "TOP", tops_path %>
      <li><%= link_to "ブログ一覧", users_path%>
      <li><%= link_to "お気に入り一覧", "/users/favorites/#{current_user.id}" %>
      <li><%= link_to "新規投稿", new_blog_path %>
      <li><%= link_to "Logout", session_path(current_user.id), method: :delete %>
    <% else %>
      <li><%= link_to "TOP", tops_path %></li>
      <li><%= link_to "Sign up", new_user_path %></li>
      <li><%= link_to "Login", new_session_path %></li>
    <% end %>
    </ul>
  </header>
  　<% flash.each do |key, value| %>
    <%= content_tag(:div, value, class: "#{key}") %>
    <% end %> 

<%= yield %>

  <footer>
    <p>DIC 201805 Shiroyama Maeka.</p>
  </footer>

</body>
</html>