<div class="container">
  <h1>My Favorites</h1>
  <p><%= notice %></p>
  <ul class="row">
    <% @favorites_blogs.each do |favorite| %>
    <li class="col-sm-6 col-md-6"><%= link_to(favorite.title, blog_path(id: favorite.id)) %></li> 
    <% end %>
  </ul>
</div>
