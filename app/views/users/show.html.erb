<h1><%= @user.name %>のブログ</h1>
<div class="container">
  <h1>記事一覧</h1>
  <p><%= notice %></p>
  <ul class="row">
    <% @user.blogs.each do |blog| %>
    <li class="col-sm-6 col-md-6"><%= link_to(blog.title, blog_path(blog.id)) %></li> 
    <% end %>
  </ul>
</div>